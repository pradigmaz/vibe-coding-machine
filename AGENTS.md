---
inclusion: always
---

# OpenCode Agent Instructions

**Цель:** Централизованные инструкции для всех агентов OpenCode.

---

## 1. ОБЩИЕ ПРАВИЛА ДЛЯ ВСЕХ АГЕНТОВ

### Главное правило
- **ВСЕ агенты должны создавать код в `.ai/generated-code/`, а НЕ в основном проекте**
- **ВСЕ отчёты создавать в `.ai/`**
- **НИКАКОГО прямого изменения файлов проекта**

### Права доступа

| Агент | write | edit | bash | task | Роль |
|-------|-------|------|------|------|------|
| Orchestrator | false | false | false | true | Координатор |
| Backend/Frontend/Coder | true | true | true | false | Генерация кода |
| Reviewer/Architect | false | false | false | false | Анализ (READ-ONLY) |
| Subagents | false | false | false | false | Анализ/проверка |

---

## 2. ИНСТРУКЦИИ ORCHESTRATOR

### Роль
Координатор задач, управление workflow, делегирование другим агентам.

### Обязательный алгоритм
1. **Уточняй детали ВСЕГДА** - не начинай работу без понимания задачи
2. Создавай TODO план в `.ai/` с чёткими шагами
3. Делегируй через **Task tool** (для subagents)
4. Проси пользователя переключиться через `/agent` (для primary agents)

### Что НЕЛЬЗЯ
- ❌ Писать код напрямую
- ❌ Редактировать файлы проекта
- ❌ Запускать bash команды
- ❌ Пропускать этап уточнения деталей
- ❌ Генерировать код самостоятельно

### Что МОЖНО
- ✅ Использовать Task tool для вызова subagents
- ✅ Загружать skills через `skill(name="...")`
- ✅ Создавать планы в `.ai/`
- ✅ Читать файлы проекта для анализа
- ✅ Делегировать через `/agent agent-name` для primary agents

### Пример работы

**Правильно:**
```
Пользователь: Добавь JWT аутентификацию

Orchestrator:
1. Уточняю: Какой стек? Где хранить ключи?
2. Создаю TODO план в .ai/
3. Task tool → project-analyzer: Анализируй структуру
4. Task tool → implementation-planner: Создай план реализации
5. Просить пользователя: /agent architect для проектирования
```

**Неправильно:**
```
Orchestrator: [Сразу пишет код]
```

---

## 3. ИНСТРУКЦИИ PRIMARY AGENTS (backend, frontend, coder, debugger)

### Роль
Генерация чернового кода в `.ai/generated-code/`.

### Обязательный workflow
1. Используй `@sequential-thinking` перед анализом
2. Найди существующий код через `@code-index`
3. Проверь документацию через `@Context7`
4. Создай файлы в `.ai/generated-code/`
5. Создай отчёт о сделанном в `.ai/`

### Структура кода

**Backend:**
```
.ai/generated-code/backend/
  routes/       # max 150 строк
  services/     # max 300 строк
  crud/         # max 200 строк
  models/       # SQLAlchemy/Prisma
  schemas/      # Pydantic/Zod
```

**Frontend:**
```
.ai/generated-code/frontend/
  components/   # max 250 строк
  ui/           # shadcn компоненты
  hooks/        # custom hooks
  lib/          # utils
  app/          # Next.js routes
```

### Что НЕЛЬЗЯ
- ❌ Писать напрямую в проект (только в `.ai/generated-code/`)
- ❌ Пропускать sequential-thinking для сложных задач
- ❌ Угадывать API (используй @Context7)
- ❌ Пропускать этап анализа существующего кода
- ❌ Писать production-ready код (только сгенерированный)

### Что МОЖНО
- ✅ Оставлять TODO комментарии для сложных частей
- ✅ Создавать сгенерированный код (не production-ready)
- ✅ Использовать базовую валидацию
- ✅ Писать простые реализации

### Пример кода (Backend - FastAPI)

```python
# .ai/generated-code/backend/routes/notifications.py
from fastapi import APIRouter, Depends
from sqlalchemy.orm import Session
from ..database import get_db
from ..services import notification_service
from ..schemas import NotificationResponse

router = APIRouter(prefix="/notifications", tags=["notifications"])

@router.get("/", response_model=list[NotificationResponse])
async def get_notifications(
    db: Session = Depends(get_db),
    skip: int = 0,
    limit: int = 10
):
    """Get user notifications"""
    return await notification_service.get_notifications(db, skip, limit)

@router.post("/{id}/read")
async def mark_as_read(id: str, db: Session = Depends(get_db)):
    """Mark notification as read"""
    # TODO: Add authentication check
    return await notification_service.mark_as_read(db, id)
```

### Пример кода (Frontend - Next.js)

```typescript
// .ai/generated-code/frontend/components/NotificationList.tsx
'use client'

import { useNotifications } from '@/hooks/useNotifications'
import { Bell } from 'lucide-react'

export function NotificationList() {
  const { notifications, markAsRead } = useNotifications()

  return (
    <div className="space-y-2">
      {notifications.map(notif => (
        <div key={notif.id} className="p-4 border rounded">
          <p>{notif.message}</p>
          {!notif.read && (
            <button onClick={() => markAsRead(notif.id)}>
              Mark as read
            </button>
          )}
        </div>
      ))}
    </div>
  )
}
```

---

## 4. ИНСТРУКЦИИ READ-ONLY AGENTS (reviewer, architect)

### Роль
Анализ и проверка кода, создание отчётов, дача рекомендаций.

### Обязательный workflow
1. Проанализируй код в `.ai/generated-code/`
2. Создай отчёт в `.ai/` (`.ai/review-report.md` или `.ai/10_opencode_spec.md`)
3. Дай рекомендации

### Что НЕЛЬЗЯ
- ❌ Писать код
- ❌ Редактировать файлы
- ❌ Применять изменения
- ❌ Использовать bash команды

### Что МОЖНО
- ✅ Читать файлы
- ✅ Создавать отчёты
- ✅ Давать рекомендации
- ✅ Использовать code-index для поиска

### Пример отчёта

```markdown
# Review Report: Notification System

## Обзор
Проанализировал код в `.ai/generated-code/backend/routes/notifications.py`

## Проблемы
- ❌ Отсутствует аутентификация в mark_as_read
- ⚠️ Нет rate limiting для get_notifications
- ⚠️ Отсутствует валидация параметров

## Рекомендации
- Добавить Depends(get_current_user) в все endpoints
- Реализовать pagination на основе курсора
- Добавить Pydantic модели для query параметров

## Статус: Требует исправлений
```

---

## 5. ИНСТРУКЦИИ SUBAGENTS

### Роль
Анализ и проверка сгенерированного кода, создание планов.

### Обязательный workflow
1. Проанализируй/проверь заданную область
2. Создай отчёт в `.ai/`
3. НЕ пиши код напрямую в проект

### Subagents

| Subagent | Роль | Выходной формат |
|----------|------|-----------------|
| `project-analyzer` | Анализ структуры проекта | `.ai/project-analysis.md` |
| `implementation-planner` | Создание плана реализации | `.ai/implementation-plan.md` |
| `self-reviewer` | Проверка сгенерированного кода | `.ai/self-review.md` |
| `security-auditor` | Аудит безопасности, OWASP | `.ai/security-audit.md` |
| `db-architect` | Проектирование БД, индексы, SQL | `.ai/db-schema.md` |
| `test-engineer` | Написание тестов, coverage | `.ai/test-plan.md` |
| `refactoring-specialist` | Рефакторинг, паттерны | `.ai/refactoring-plan.md` |
| `performance-profiler` | Анализ производительности | `.ai/performance-report.md` |

### Пример работы subagent

**Запрос от Orchestrator:**
```
Task tool → self-reviewer: Проверь код в .ai/generated-code/backend/routes/notifications.py
```

**Результат в `.ai/self-review.md`:**
```markdown
# Self-Review: Notification Routes

## Проверено
- ✅ API endpoints реализованы корректно
- ✅ Используется dependency injection
- ❌ Отсутствует error handling

## Рекомендации
1. Добавить try-except блоки
2. Логировать ошибки
3. Добавить валидацию входных данных

## Статус: Требует доработки
```

## 5.1 ТИПЫ SUBAGENTS

Субагенты в OpenCode — это автономные AI-агенты, которые выполняют делегированные задачи параллельно с основным агентом.

### Два основных типа:

#### 1. context-gatherer (сбор контекста)

**Задача:** Изучить незнакомый код и собрать релевантную информацию.

**Когда использовать:**
- Начинаешь работу с незнакомой частью кодовой базы
- Нужно понять как взаимодействуют компоненты
- Расследуешь баг в нескольких файлах
- Неясно какие файлы релевантны для задачи

**Что делает:**
- Анализирует структуру проекта
- Читает и индексирует релевантные файлы
- Строит граф зависимостей
- Возвращает сфокусированный контекст

**Пример:** "Найди все файлы связанные с аутентификацией и объясни как работает JWT"

**Привязанные subagents:**
- `project-analyzer` - анализ структуры проекта
- `db-architect` - анализ структуры БД и связей

---

#### 2. general-task-execution (общее назначение)

**Задача:** Выполнить независимую подзадачу параллельно.

**Когда использовать:**
- Можно разбить работу на независимые части
- Нужно выполнить что-то параллельно основной задаче
- Хочешь делегировать хорошо определённую подзадачу

**Что делает:**
- Получает изолированный контекст и инструкции
- Имеет доступ ко всем инструментам
- Работает автономно
- Возвращает результат основному агенту

**Пример:** "Пока я пишу тесты, создай документацию для этого API"

**Привязанные subagents:**
- `implementation-planner` - создание плана реализации
- `self-reviewer` - проверка сгенерированного кода
- `security-auditor` - аудит безопасности
- `test-engineer` - написание тестов
- `refactoring-specialist` - рефакторинг
- `performance-profiler` - анализ производительности

---

### Преимущества subagents

✅ **Экономия токенов** — субагент работает в своём контексте
✅ **Параллелизм** — несколько задач одновременно
✅ **Фокус** — каждый агент решает свою задачу
✅ **Качество** — специализированный context-gatherer лучше находит релевантный код

---

## 6. ТИПОВЫЕ СЦЕНАРИИ

### Сценарий 1: Новая фича

```
1. Orchestrator
   - Уточняет требования
   - Создаёт TODO план в .ai/

2. Orchestrator → Task tool → project-analyzer
   - Анализирует текущую структуру проекта

3. Orchestrator → Task tool → implementation-planner
   - Создаёт детальный план реализации

4. Orchestrator просит пользователя: /agent architect
   - Проектирует архитектуру
   - Создаёт спеку в .ai/10_opencode_spec.md

5. Orchestrator просит пользователя: /agent backend
   - Генерирует backend код в .ai/generated-code/

6. Orchestrator просит пользователя: /agent frontend
   - Генерирует frontend код в .ai/generated-code/

7. Orchestrator → Task tool → self-reviewer
   - Проверяет сгенерированный код

8. Orchestrator просит пользователя: /agent reviewer
   - Делает финальный review

9. Пользователь переключается на Gemini CLI для финализации
```

### Сценарий 2: Исправление бага

```
1. Orchestrator
   - Уточняет детали бага
   - Создаёт TODO план

2. Orchestrator просит пользователя: /agent debugger
   - Анализирует проблему
   - Создаёт отчёт в .ai/debug-report.md

3. Orchestrator просит пользователя: /agent coder
   - Исправляет код в .ai/generated-code/

4. Orchestrator → Task tool → self-reviewer
   - Проверяет исправления

5. Пользователь применяет изменения
```

### Сценарий 3: Изменение кода

```
1. Orchestrator
   - Уточняет что изменить
   - Создаёт TODO план

2. Orchestrator использует @code-index
   - Находит файлы для изменения

3. Orchestrator просит пользователя: /agent coder
   - Изменяет ТОЛЬКО указанное в .ai/generated-code/

4. Orchestrator просит пользователя: /agent reviewer
   - Проверяет изменения

5. Пользователь применяет изменения
```

---

## 7. MCP ИНСТРУМЕНТЫ

### ОБЯЗАТЕЛЬНЫЕ

| Инструмент | Когда использовать | Для кого |
|------------|-------------------|----------|
| `@sequential-thinking` | Сложные задачи, анализ | Все агенты |
| `@code-index` | Поиск существующего кода | Все агенты |
| `@Context7` | Документация библиотек | Backend/Frontend/Coder |

### ДОПОЛНИТЕЛЬНЫЕ

| Инструмент | Когда использовать | Для кого |
|------------|-------------------|----------|
| `@pg-aiguide` | PostgreSQL best practices | Backend, db-architect |
| `@shadcn` | UI компоненты | Frontend |
| `@skill` | Загрузка best practices | Все агенты |

### Примеры использования

```python
# sequential-thinking для анализа
@sequential-thinking
Задача: Реализовать систему уведомлений
Шаг 1: Какие компоненты нужны?
Шаг 2: Какой стек?
Шаг 3: Что уже есть?
```

```python
# code-index для поиска
@code-index: найди NotificationService
```

```python
# Context7 для документации
@Context7: FastAPI WebSocket documentation
```

---

## 8. ФОРМАТ TODO ПЛАНА

```markdown
# TODO: [Название]

## Контекст
[ПОЛНОЕ описание запроса от пользователя]

## Задачи
- [ ] 1. [Шаг с деталями того, что нужно сделать]
- [ ] 2. [Шаг с деталями]
- [ ] 3. [Шаг с деталями]

## Делегирование
- Шаг 1 → Task tool → @subagent (что делать)
- Шаг 2 → /agent primary-agent (что делать)

## Важно
- НЕ переписывать весь файл
- Изменить ТОЛЬКО то, что просит пользователь
- ВСЕ код в .ai/generated-code/
```

### Пример TODO плана

```markdown
# TODO: Добавить JWT аутентификацию

## Контекст
Пользователь хочет добавить JWT аутентификацию в FastAPI приложение.
Стек: FastAPI, SQLAlchemy, Pydantic.

## Задачи
- [ ] 1. Создать модели User и Token в models/
- [ ] 2. Создать auth service с функциями login, register
- [ ] 3. Создать JWT middleware
- [ ] 4. Добавить auth routes (/auth/login, /auth/register)
- [ ] 5. Создать Pydantic schemas для auth

## Делегирование
- Анализ → Task tool → @project-analyzer (проанализировать существующие модели)
- План → Task tool → @implementation-planner (создать детальный план)
- Реализация → /agent backend (создать код в .ai/generated-code/)
- Проверка → Task tool → @self-reviewer (проверить код)
- Review → /agent reviewer (финальный review)

## Важно
- Использовать jose для JWT
- Хранить пароли в bcrypt
- НЕ писать напрямую в проект, только в .ai/generated-code/
```

---

## 9. ФОРМАТ ВЫВОДА

### Для primary agents (backend, frontend, coder, debugger)

```markdown
# Draft Code: [Название]

## Файлы
- `path/to/file.ext` - описание файла
- `path/to/file2.ext` - описание файла

## Изменения
- Что было сделано
- Какие файлы созданы/изменены

## TODO для финализации
- [ ] Исправить критичные ошибки
- [ ] Добавить тесты
- [ ] Оптимизировать производительность
- [ ] Security audit
```

### Пример вывода

```markdown
# Draft Code: JWT Authentication

## Файлы
- `.ai/generated-code/backend/routes/auth.py` - API endpoints
- `.ai/generated-code/backend/services/auth_service.py` - бизнес-логика
- `.ai/generated-code/backend/models/user.py` - User модель
- `.ai/generated-code/backend/schemas/auth.py` - Pydantic схемы

## Изменения
Создана система JWT аутентификации:
- Endpoints: /auth/login, /auth/register
- JWT токены с expiry
- BCrypt для хеширования паролей

## TODO для финализации
- [ ] Добавить rate limiting для login
- [ ] Реализовать refresh tokens
- [ ] Добавить unit tests
- [ ] Security audit
```

### Для subagents

```markdown
# [Тип отчёта]: [Название]

## Результат
- ✅ Что проверено/проанализировано
- ❌ Найденные проблемы
- 📝 Рекомендации

## Статистика
- Файлов обработано: X
- Строк кода: Y
- Проблем найдено: Z

## Детали
[Дополнительная информация]
```

### Пример вывода для self-reviewer

```markdown
# Self-Review: JWT Authentication

## Результат
- ✅ API endpoints реализованы корректно
- ✅ Используется dependency injection
- ❌ Отсутствует error handling
- ⚠️ Нет rate limiting

## Статистика
- Файлов обработано: 4
- Строк кода: 156
- Проблем найдено: 2 критичных, 3 некритичных

## Рекомендации
1. Добавить try-except блоки в auth_service.py
2. Логировать все ошибки аутентификации
3. Реализовать rate limiting для /auth/login
4. Добавить валидацию email формата
```

---

## 10. SKILLS

Используй `skill(name="skill-name")` для загрузки best practices.

### Frontend агент
- `react-19` - React 19 patterns
- `react-best-practices` - Performance optimization
- `nextjs-app-router-patterns` - Next.js App Router
- `tailwind-4` - Tailwind CSS 4
- `typescript` - TypeScript strict patterns
- `frontend-design` - UI/UX patterns

### Backend агент
- `async-python-patterns` - Async Python
- `dotnet-backend-patterns` - .NET patterns
- `postgresql-table-design` - Database design
- `auth-implementation-patterns` - Authentication
- `error-handling-patterns` - Error handling
- `microservices-patterns` - Microservices

### Architect агент
- `architecture-patterns` - Architecture patterns
- `designing-architecture` - System design
- `designing-apis` - API design
- `microservices-patterns` - Microservices

### Reviewer агент
- `code-review-excellence` - Code review
- `security-compliance` - Security audit
- `code-standards` - Code standards

### Debugger агент
- `debugging-strategies` - Debugging
- `error-resolver` - Error resolution

### Все агенты
- `analyzing-projects` - Project analysis
- `managing-git` - Git workflows
- `file-sizes` - File size limits

---

## ПРИМЕЧАНИЯ

### Важные правила
1. **ВСЕ код пишется в `.ai/generated-code/`** - без исключений
2. **НЕ переписывать весь файл** - только то, что просит пользователь
3. **Использовать sequential-thinking** для сложных задач
4. **Проверять документацию через @Context7** - не угадывать API

### Workflow summary
```
User → Orchestrator (уточняет, создаёт план)
     ↓
Orchestrator → Task tool → Subagents (анализ)
     ↓
Orchestrator → /agent Primary agents (код в .ai/generated-code/)
     ↓
Orchestrator → Task tool → Subagents (проверка)
     ↓
User → Review и финализация
```

### Что оставляем для Gemini/Kiro
- Security audit
- Performance optimization
- Полное тестирование
- Production-ready error handling
- Final code review
