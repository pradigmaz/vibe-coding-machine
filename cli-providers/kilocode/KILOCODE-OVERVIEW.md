# KiloCode CLI - Полный обзор

## Что такое KiloCode

KiloCode - это самая популярная open source платформа для agentic разработки, которая позволяет строить, деплоить и итерировать быстрее с помощью AI агентов.

**Ключевые особенности**:
- OpenRouter + режим оркестратора
- Доступ к 500+ моделям
- #1 на OpenRouter
- 1M+ пользователей (Kilo Coders)
- 20T+ обработанных токенов
- Open source (Apache-2.0)

## Официальные ресурсы

- **Официальный сайт**: https://kilocode.ai
- **Документация**: https://kilocode.ai/docs/
- **CLI документация**: https://kilocode.ai/docs/cli
- **GitHub**: https://github.com/Kilo-Org/kilocode
- **Discord**: Сообщество разработчиков

## Философия Kilo Speed

**Kilo Speed** - это состояние легкого, радостного потока, достигаемое когда вы можете фокусироваться без зависимостей, блокеров или трения любого рода.

### Новая парадигма разработки

**Старая парадигма (2023)**:
- Человек пишет код, AI помогает с автодополнением
- Линейные workflow с ручными переходами
- Один инструмент = одна задача
- Контекст теряется при переключении

**Новая парадигма (2028)**:
- AI пишет код, человек направляет и проверяет
- Параллельные workflow с оркестрацией
- Одна платформа = все задачи
- Контекст сохраняется везде

## Установка

### Через npm (рекомендуется)

```bash
npm install -g @kilocode/cli
```

### Запуск

```bash
cd your-project
kilocode
```

## Обновление

```bash
npm update -g @kilocode/cli
```


## Основные возможности

### 1. Режимы работы (Modes)

KiloCode предлагает 5 встроенных режимов для разных задач:

#### Code Mode (по умолчанию)
- Полный доступ ко всем инструментам
- Написание кода, реализация функций
- Отладка и общая разработка

#### Ask Mode
- Только чтение и браузер
- Объяснение кода
- Изучение концепций
- Не изменяет код

#### Architect Mode
- Проектирование систем
- Создание планов реализации
- Может редактировать только markdown
- Структурированный подход

#### Debug Mode
- Систематическая отладка
- Диагностика ошибок
- Полный доступ к инструментам
- Методичный подход

#### Orchestrator Mode
- Координация сложных задач
- Делегирование подзадач
- Разбиение проектов на части
- Использует специализированные режимы

### 2. Доступ к 500+ моделям

Через OpenRouter и BYOK (Bring Your Own Key):
- Anthropic (Claude)
- OpenAI (GPT-4, GPT-5.1)
- Google (Gemini)
- Meta (Llama)
- Mistral
- Cohere
- И многие другие

### 3. Orchestrator Mode

Разбивает сложные проекты на управляемые части:
- Каждая подзадача в своем контексте
- Разные режимы для разных задач
- Родительская задача получает только резюме
- Эффективное управление контекстом

### 4. Kilo Sessions

Бесшовная работа между устройствами:
- Начните на телефоне
- Продолжите в VS Code
- Завершите в CLI
- Контекст следует за вами

### 5. Skills (Навыки)

Расширение возможностей AI:
- Специализированные знания
- Повторяемые workflow
- Sharable пакеты
- Community skills

### 6. Memory Bank

Сохранение контекста:
- Архитектурные решения
- Предпочтения разработчика
- История работы
- Автоматическая онбординг команды

### 7. Parallel Agents

Множественные агенты одновременно:
- Работа над разными частями кода
- Git worktrees для изоляции
- Pull requests для review
- Драматическое ускорение работы

### 8. Cloud Agents

Выполнение в облаке:
- Не потребляет локальные ресурсы
- Долгие задачи
- Сложные workflow
- Ресурсоемкие операции

### 9. Code Review

Автоматический review:
- Анализ pull requests
- Предложения улучшений
- Обнаружение багов
- Проверка качества кода

### 10. Auto Top-Ups

Автоматическое пополнение:
- Никогда не заканчиваются кредиты
- Автопополнение при < $5
- Непрерывный workflow


## CLI Команды

### Основные команды

```bash
kilocode                    # Запуск интерактивного режима
kilocode config             # Открыть редактор конфигурации
```

### Slash команды в CLI

#### Режимы
```bash
/mode                       # Переключение между режимами
/mode orchestrator          # Переключиться на orchestrator
```

#### Модели
```bash
/model list                 # Список доступных моделей
/model info <name>          # Информация о модели
/model select               # Выбрать модель
```

Пример:
```bash
/model info z-ai/glm-4.5v
/model select
```

#### Checkpoint Management
```bash
/checkpoint list            # Список checkpoints
/checkpoint restore <hash>  # Восстановить checkpoint
/cp list                    # Короткая версия
```

#### Task History
```bash
/tasks                      # История задач
/tasks search <query>       # Поиск задач
/tasks select <id>          # Выбрать задачу
/tasks page <n>             # Страница N
/tasks next                 # Следующая страница
/tasks prev                 # Предыдущая страница
/tasks sort <order>         # Сортировка
/tasks filter <type>        # Фильтр
/t                          # Короткая версия
/history                    # Альтернатива
```

Примеры:
```bash
/tasks search bug fix
/tasks sort most-expensive
/tasks filter favorites
```

#### Teams
```bash
/teams list                 # Список организаций
/teams select               # Переключить организацию
```

#### Утилиты
```bash
/config                     # Открыть конфигурацию
/new                        # Новая задача с чистым slate
/help                       # Список команд
/exit                       # Выход из CLI
```

### Горячие клавиши

| Клавиша | Действие |
|---------|----------|
| `Shift+Tab` | Переключение режимов (architect → code → ask → debug → orchestrator → custom) |
| `Ctrl+C` | Выход (дважды для подтверждения) |
| `Ctrl+X` | Отменить текущую задачу |
| `Esc` | Отменить задачу (во время streaming) или очистить ввод |
| `Ctrl+Y` | Toggle YOLO mode (авто-одобрение всех операций) |
| `Ctrl+R` | Возобновить задачу |
| `!` | Войти в shell mode (когда ввод пуст) |
| `↑/↓` | Навигация по истории команд (когда ввод пуст) |

### Shell Mode

Когда ввод пуст, введите `!` для входа в shell mode:
```bash
!
# Теперь можно выполнять shell команды
ls -la
git status
npm test
```


## Режимы работы (Modes)

### Code Mode

**Описание**: Опытный software engineer с экспертизой в языках программирования, паттернах проектирования и best practices.

**Доступ к инструментам**: Полный доступ ко всем группам инструментов (read, edit, browser, command, mcp)

**Идеально для**:
- Написание кода
- Реализация функций
- Отладка
- Общая разработка

**Особенности**: Нет ограничений на инструменты - полная гибкость для всех задач кодирования

### Ask Mode

**Описание**: Технический ассистент, фокусирующийся на ответах на вопросы без изменения кодовой базы.

**Доступ к инструментам**: Ограниченный доступ (read, browser, mcp только - не может редактировать файлы или выполнять команды)

**Идеально для**:
- Объяснение кода
- Изучение концепций
- Техническое обучение

**Особенности**: Оптимизирован для информативных ответов без модификации проекта

### Architect Mode

**Описание**: Опытный технический лидер и планировщик, помогающий проектировать системы и создавать планы реализации.

**Доступ к инструментам**: Доступ к read, browser, mcp и ограниченный edit (только markdown файлы)

**Идеально для**:
- Проектирование систем
- Высокоуровневое планирование
- Архитектурные обсуждения

**Особенности**: Следует структурированному подходу от сбора информации до детального планирования

### Debug Mode

**Описание**: Эксперт по решению проблем, специализирующийся на систематической отладке и диагностике.

**Доступ к инструментам**: Полный доступ ко всем группам инструментов (read, edit, browser, command, mcp)

**Идеально для**:
- Отслеживание багов
- Диагностика ошибок
- Решение сложных проблем

**Особенности**: Использует методичный подход анализа, сужения возможностей и исправления проблем

### Orchestrator Mode

**Описание**: Стратегический оркестратор workflow, координирующий сложные задачи путем делегирования их соответствующим специализированным режимам.

**Доступ к инструментам**: Ограниченный доступ для создания новых задач и координации workflow

**Идеально для**:
- Разбиение сложных проектов на управляемые подзадачи
- Назначение задач специализированным режимам

**Особенности**: Использует инструмент `new_task` для делегирования работы другим режимам

**Как работает**:
1. Анализирует сложную задачу и предлагает разбить на подзадачи
2. Родительская задача приостанавливается, начинается новая подзадача в другом режиме
3. Когда цель подзадачи достигнута, Kilo сигнализирует о завершении
4. Родительская задача возобновляется только с резюме подзадачи

**Ключевые соображения**:
- Подзадачи не имеют доступа к контексту родительской задачи
- Резюме должно быть информативным для продолжения работы
- Эффективно для управления большими контекстами

### Custom Modes

Создавайте собственных специализированных ассистентов:
- Определение доступа к инструментам
- Настройка разрешений файлов
- Инструкции поведения
- Соблюдение стандартов команды

См. документацию Custom Modes для настройки.

### Переключение между режимами

**4 способа переключения**:

1. **Dropdown меню**: Клик на селектор слева от chat input
2. **Slash команда**: `/architect`, `/ask`, `/debug`, `/code` в chat
3. **Keyboard shortcut**: 
   - macOS: `⌘ + .`
   - Windows: `Ctrl + .`
   - Linux: `Ctrl + .`
   - CLI: `Shift+Tab`
4. **Предложения**: Клик на предложения переключения режима от Kilo


## Skills (Навыки)

### Что такое Skills

Skills - это легковесный формат для расширения возможностей AI со специализированными знаниями и workflow.

### Расположение Skills

Skills обнаруживаются из:
- **Global**: `~/.kilocode/skills/`
- **Project**: `.kilocode/skills/`

### Структура Skill

**Директория**: `.kilocode/skills/my-skill/`

**Файл**: `SKILL.md`

```markdown
---
name: my-skill
description: Brief description of what this skill does
---

# My Skill

Detailed instructions and context for the AI agent.

## When to use

- Use case 1
- Use case 2

## How to use

Step-by-step instructions...

## Examples

Example code or workflows...
```

**Важно**: Поле `name` должно точно совпадать с именем директории.

### Типы Skills

Skills могут быть:
- **Loaded automatically** - Автоматически загружаются при запуске
- **Loaded on-demand** - Загружаются по запросу через команду

### Создание Skill

1. Создать директорию skill:
```bash
mkdir -p .kilocode/skills/my-skill
```

2. Создать `SKILL.md` с YAML frontmatter:
```markdown
---
name: my-skill
description: My custom skill
---

# My Skill Content
```

3. Запустить новую CLI сессию для загрузки skill

### Поиск Skills

Community ресурсы для поиска и шаринга skills:
- GitHub репозитории
- Kilo Discord сообщество
- Community skill collections

### Примеры Skills

#### Code Review Skill
```markdown
---
name: code-review
description: Comprehensive code review guidelines
---

# Code Review Skill

## Review Checklist

- [ ] Code follows project style guide
- [ ] Tests are included
- [ ] Documentation is updated
- [ ] No security vulnerabilities
- [ ] Performance considerations addressed

## Common Issues

1. **Missing error handling**
2. **Hardcoded values**
3. **Inefficient algorithms**
```

#### API Design Skill
```markdown
---
name: api-design
description: RESTful API design best practices
---

# API Design Skill

## Principles

- Use nouns for resources
- Use HTTP methods correctly
- Version your API
- Provide clear error messages

## Example Structure

```
GET    /api/v1/users
POST   /api/v1/users
GET    /api/v1/users/:id
PUT    /api/v1/users/:id
DELETE /api/v1/users/:id
```
```


## Checkpoint Management

### Что такое Checkpoints

Kilo Code автоматически создает checkpoints во время работы, позволяя вернуться к предыдущим состояниям проекта.

### Просмотр Checkpoints

```bash
/checkpoint list
# или короткая версия
/cp list
```

Показывает:
- Git hash
- Дата и время
- Описание изменений
- Размер изменений

### Восстановление Checkpoint

```bash
/checkpoint restore <full-git-hash>
# или
/cp restore abc123def456...
```

**Важно**: Это деструктивная операция - текущие изменения будут потеряны.

### Примеры использования

```bash
# Список всех checkpoints
/checkpoint list

# Восстановить конкретный checkpoint
/checkpoint restore 41db173a8f2e...

# Быстрый доступ
/cp list
/cp restore 41db173a8f2e...
```

## Task History

### Просмотр истории задач

```bash
/tasks
# или короткие версии
/t
/history
```

Показывает:
- ID задачи
- Заголовок
- Дата создания
- Статус
- Стоимость
- Использованные токены

### Поиск задач

```bash
/tasks search <query>
```

Примеры:
```bash
/tasks search bug fix
/tasks search authentication
/tasks search refactor
```

Поиск автоматически сортирует результаты по релевантности.

### Выбор задачи

```bash
/tasks select <task-id>
```

Загружает выбранную задачу и полную историю разговора.

### Навигация по страницам

```bash
/tasks page <n>     # Перейти на страницу N
/tasks next         # Следующая страница
/tasks prev         # Предыдущая страница
```

### Сортировка задач

```bash
/tasks sort <order>
```

Варианты сортировки:
- `most-recent` - Самые свежие
- `most-expensive` - Самые дорогие
- `most-tokens` - Больше всего токенов
- `alphabetical` - По алфавиту

### Фильтрация задач

```bash
/tasks filter <type>
```

Варианты фильтров:
- `favorites` - Избранные задачи
- `workspace` - По workspace
- `mode` - По режиму
- `model` - По модели

### Примеры workflow

```bash
# Найти все задачи по багам
/tasks search bug

# Отсортировать по стоимости
/tasks sort most-expensive

# Показать только избранные
/tasks filter favorites

# Выбрать конкретную задачу
/tasks select abc123

# Навигация
/tasks next
/tasks prev
/tasks page 3
```


## Конфигурация

### Редактор конфигурации

```bash
kilocode config
```

Интерактивный workflow для настройки:
- Провайдеры AI
- API ключи
- Auto-approval настройки
- MCP серверы
- Режимы работы

### Расположение конфигурации

- **Global**: `~/.kilocode/config.json`
- **Project**: `.kilocode/config.json`
- **MCP Global**: `~/.kilocode/mcp_settings.json`
- **MCP Project**: `.kilocode/mcp.json`

### Провайдеры

#### OpenRouter (рекомендуется)

**Получение API ключа**:
1. Перейти на https://openrouter.ai/
2. Войти через Google или GitHub
3. Перейти на страницу keys
4. Скопировать API ключ

**Конфигурация**:
```json
{
  "apiProvider": "openrouter",
  "openrouterApiKey": "sk-or-v1-...",
  "model": "anthropic/claude-3.5-sonnet"
}
```

**Особенности**:
- Доступ к 500+ моделям
- Единый API для всех провайдеров
- Автоматическое роутинг
- Prompt compression
- Prompt caching поддержка

**Provider Routing**:
- Default (настройки аккаунта)
- Prefer lower price
- Prefer higher throughput
- Prefer lower latency
- Specific provider

**Data Policy**:
- No data policy set (настройки аккаунта)
- Allow prompt training
- Deny prompt training
- Zero data retention

#### Anthropic

Прямой доступ к Claude моделям.

```json
{
  "apiProvider": "anthropic",
  "anthropicApiKey": "sk-ant-...",
  "model": "claude-3.5-sonnet"
}
```

#### OpenAI

```json
{
  "apiProvider": "openai",
  "openaiApiKey": "sk-...",
  "model": "gpt-4-turbo"
}
```

#### Google

```json
{
  "apiProvider": "google",
  "googleApiKey": "...",
  "model": "gemini-2.0-flash"
}
```

#### Другие провайдеры

- AWS Bedrock
- Azure OpenAI
- Vercel AI Gateway
- Groq
- Cohere
- Mistral
- Локальные модели (Ollama, LM Studio)

### Auto-Approval настройки

Конфигурация автоматического одобрения операций.

**Расположение**: `~/.kilocode/config.json`

```json
{
  "autoApproval": {
    "enabled": true,
    "read": {
      "enabled": true,
      "outside": false
    },
    "write": {
      "enabled": true,
      "outside": false,
      "protected": false
    },
    "execute": {
      "enabled": true,
      "allowed": ["npm", "git", "pnpm"],
      "denied": ["rm -rf", "sudo"]
    },
    "browser": {
      "enabled": false
    },
    "mcp": {
      "enabled": true
    },
    "mode": {
      "enabled": true
    },
    "subtasks": {
      "enabled": true
    },
    "question": {
      "enabled": false,
      "timeout": 60
    },
    "retry": {
      "enabled": true,
      "delay": 10
    },
    "todo": {
      "enabled": true
    }
  }
}
```

**Параметры**:

- `enabled` - Включить/выключить auto-approval
- `read.enabled` - Авто-одобрение чтения файлов
- `read.outside` - Чтение файлов вне workspace
- `write.enabled` - Авто-одобрение записи файлов
- `write.outside` - Запись файлов вне workspace
- `write.protected` - Запись защищенных файлов
- `execute.enabled` - Авто-одобрение команд
- `execute.allowed` - Список разрешенных команд
- `execute.denied` - Список запрещенных команд
- `browser.enabled` - Авто-одобрение браузера
- `mcp.enabled` - Авто-одобрение MCP инструментов
- `mode.enabled` - Авто-одобрение переключения режимов
- `subtasks.enabled` - Авто-одобрение подзадач
- `question.enabled` - Авто-ответ на вопросы
- `question.timeout` - Таймаут для вопросов (секунды)
- `retry.enabled` - Авто-повтор при ошибках
- `retry.delay` - Задержка перед повтором (секунды)
- `todo.enabled` - Авто-одобрение todo операций

### Command Approval Patterns

Списки `execute.allowed` и `execute.denied` поддерживают иерархическое сопоставление паттернов:

**Примеры**:
```json
{
  "execute": {
    "allowed": [
      "npm",           // Разрешить все npm команды
      "git status",    // Только git status
      "pnpm"          // Все pnpm команды
    ],
    "denied": [
      "rm -rf",       // Запретить rm -rf
      "sudo",         // Запретить sudo
      "git push"      // Запретить git push
    ]
  }
}
```

**Правила**:
- Более специфичные паттерны имеют приоритет
- `denied` имеет приоритет над `allowed`
- Паттерны проверяются в порядке специфичности


## Режимы выполнения

### Interactive Mode (по умолчанию)

Интерактивный режим для работы с пользователем через консоль.

**Характеристики**:
- Запрос одобрения для операций
- Возможность review перед выполнением
- Добавление в auto-approval список
- Прогрессивное построение правил

**Interactive Command Approval**:

При запросе одобрения команды показываются иерархические опции:
- `Run once` - Выполнить один раз
- `Always run <specific-command>` - Всегда выполнять эту команду
- `Always run <command-prefix>*` - Всегда выполнять все команды с префиксом
- `Deny` - Отклонить

Выбор "Always run" опции:
1. Одобряет и выполняет текущую команду
2. Добавляет паттерн в `execute.allowed` в конфиге
3. Авто-одобряет совпадающие команды в будущем

### Autonomous Mode (Non-Interactive)

Автономный режим для автоматизированных сред (CI/CD).

**Запуск**:
```bash
kilocode --auto
```

**Поведение**:
- Нет взаимодействия с пользователем
- Авто-одобрение/отклонение на основе конфигурации
- Автоматические ответы на вопросы
- Автоматический выход при завершении

**Follow-up Questions**:

В автономном режиме AI получает ответ:
```
"This process is running in non-interactive Autonomous mode. 
The user cannot make decisions, so you should make the decision autonomously."
```

**Auto-Approval в Autonomous Mode**:

Автономный режим уважает конфигурацию auto-approval. Операции без auto-approval не будут разрешены.

### JSON Output Mode

Структурированный JSON вывод для программной интеграции.

**Запуск**:
```bash
kilocode --auto --json
```

**Требования**:
- Должен использоваться с `--auto` флагом
- Вывод в JSON формате вместо TUI

**Использование**:
- Парсинг ответов Kilo Code
- Интеграция в CI/CD
- Автоматизированные workflow

### Exit Codes

| Код | Описание |
|-----|----------|
| 0 | Успешное завершение |
| 1 | Общая ошибка |
| 2 | Ошибка конфигурации |
| 3 | Ошибка аутентификации |
| 4 | Таймаут |
| 5 | Отменено пользователем |

### Пример CI/CD интеграции

```yaml
name: Kilo Code CI
on: [push, pull_request]

jobs:
  kilo-review:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      
      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '18'
      
      - name: Install Kilo Code
        run: npm install -g @kilocode/cli
      
      - name: Run Kilo Code Review
        env:
          OPENROUTER_API_KEY: ${{ secrets.OPENROUTER_API_KEY }}
        run: |
          kilocode --auto --json << EOF
          Review this pull request for:
          - Code quality
          - Security issues
          - Performance problems
          - Best practices
          EOF
```


## Session Continuation

Возобновление последнего разговора из текущего workspace.

**Запуск**:
```bash
kilocode --continue
# или короткая версия
kilocode -c
```

**Возможности**:
- Восстанавливает полную историю разговора
- Сохраняет контекст задачи
- Продолжает с того же режима
- Использует ту же модель

**Пример workflow**:

```bash
# День 1: Начать задачу
kilocode
> Implement user authentication

# День 2: Продолжить
kilocode -c
> Add password reset functionality

# День 3: Продолжить снова
kilocode -c
> Add two-factor authentication
```

**Ограничения**:
- Работает только в текущем workspace
- Одна активная сессия на workspace
- История хранится локально

## Parallel Mode

Множественные Kilo Code инстансы работают параллельно на одной директории без конфликтов.

**Возможности**:
- Spawn множество инстансов
- Работа на разных частях кода
- Использование git worktrees
- Изменения на отдельных ветках
- Review как pull requests

**Запуск**:
```bash
# Терминал 1
kilocode
> Implement feature A

# Терминал 2
kilocode
> Implement feature B

# Терминал 3
kilocode
> Fix bug C
```

**Как работает**:
1. Каждый инстанс создает git worktree
2. Работа изолирована в отдельной ветке
3. По завершении - pull request
4. Review и merge независимо

**Преимущества**:
- Драматическое ускорение работы
- Параллельная разработка функций
- Независимые review
- Нет конфликтов

## Environment Variable Overrides

CLI поддерживает переопределение конфигурации через переменные окружения.

**Поддерживаемые переменные**:

```bash
# API ключи
OPENROUTER_API_KEY=sk-or-v1-...
ANTHROPIC_API_KEY=sk-ant-...
OPENAI_API_KEY=sk-...
GOOGLE_API_KEY=...

# Конфигурация
KILOCODE_CONFIG_PATH=~/.kilocode/custom-config.json
KILOCODE_AUTO_APPROVE=true
KILOCODE_MODE=architect
KILOCODE_MODEL=anthropic/claude-3.5-sonnet

# Логирование
KILOCODE_LOG_LEVEL=debug
KILOCODE_LOG_FILE=~/.kilocode/logs/cli.log

# Прокси
HTTP_PROXY=http://proxy.example.com:8080
HTTPS_PROXY=https://proxy.example.com:8080
```

**Примеры использования**:

```bash
# Использовать конкретную модель
KILOCODE_MODEL=google/gemini-2.0-flash kilocode

# Включить debug логи
KILOCODE_LOG_LEVEL=debug kilocode

# Использовать другой конфиг
KILOCODE_CONFIG_PATH=./custom-config.json kilocode

# Авто-одобрение всех операций
KILOCODE_AUTO_APPROVE=true kilocode --auto
```

## Local Development

### DevTools

Запуск CLI с devtools:

```bash
DEV=true pnpm start
```

Затем в другом терминале:
```bash
npx react-devtools
```

Показывает devtools inspector для отладки.

### Switching Organizations

Переключение между организациями из CLI:

```bash
# Список организаций
/teams list

# Выбрать организацию
/teams select
# Начните вводить имя команды для поиска
```

Процесс одинаков для Team и Enterprise организаций.


## MCP (Model Context Protocol)

### Что такое MCP

MCP расширяет возможности Kilo Code, подключая внешние инструменты и сервисы.

### Конфигурация MCP серверов

**Два уровня конфигурации**:

1. **Global Configuration**: `~/.kilocode/mcp_settings.json`
   - Применяется ко всем workspace
   - Доступна через VS Code settings

2. **Project Configuration**: `.kilocode/mcp.json`
   - Специфична для проекта
   - Можно коммитить в git
   - Автоматически обнаруживается

**Приоритет**: Project конфигурация переопределяет global.

### Редактирование MCP настроек

**Через UI**:
1. Клик на ⚙️ в Kilo Code панели
2. MCP Servers tab
3. Installed servers
4. Клик Edit Global Config или Edit Project Config

**Формат JSON**:
```json
{
  "mcpServers": {
    "server-name": {
      "type": "stdio|http|sse",
      "command": ["node", "server.js"],
      "env": {
        "API_KEY": "..."
      }
    }
  }
}
```

### Типы транспорта

#### STDIO Transport

Для локальных серверов на вашей машине:

```json
{
  "mcpServers": {
    "local-server": {
      "type": "stdio",
      "command": ["node", "path/to/server.js"],
      "args": ["--port", "3000"],
      "env": {
        "NODE_ENV": "production"
      }
    }
  }
}
```

#### HTTP Stream Transport

Для удаленных серверов через HTTP/HTTPS:

```json
{
  "mcpServers": {
    "remote-server": {
      "type": "http",
      "url": "https://api.example.com/mcp",
      "headers": {
        "Authorization": "Bearer token"
      }
    }
  }
}
```

#### SSE Transport (DEPRECATED)

⚠️ Устарел с MCP спецификации 2025-03-26. Используйте HTTP Stream Transport.

```json
{
  "mcpServers": {
    "sse-server": {
      "type": "sse",
      "url": "https://api.example.com/sse"
    }
  }
}
```

### Управление серверами

**Удаление сервера**:
1. Нажать 🗑️ рядом с MCP сервером
2. Подтвердить удаление

**Перезапуск сервера**:
- Нажать 🔄 рядом с MCP сервером

**Включение/отключение сервера**:
- Переключить 🔘 рядом с MCP сервером

**Network Timeout**:
- Клик на Network Timeout pulldown
- Выбрать время (30 сек - 5 мин)
- По умолчанию: 1 минута

### Auto Approve Tools

MCP tool auto-approval работает per-tool и отключен по умолчанию.

**Настройка**:
1. Включить глобальную опцию "Use MCP servers" в auto-approving-actions
2. В настройках MCP сервера найти нужный tool
3. Отметить "Always allow" рядом с tool

**Важно**: Глобальная настройка "Use MCP servers" имеет приоритет.

### Поиск и установка MCP серверов

Kilo Code не поставляется с предустановленными MCP серверами.

**Ресурсы**:
- MCP GitHub repository: https://github.com/modelcontextprotocol
- Community MCP servers
- Official MCP marketplace

**Документация**: https://github.com/modelcontextprotocol

### Использование MCP инструментов

После конфигурации MCP сервера:

1. Введите запрос в Kilo Code chat
2. Kilo Code определит когда MCP tool может помочь
3. Одобрите использование tool (или используйте auto-approval)

**Пример**:
```
Analyze the performance of my API
```

Может использовать MCP tool для тестирования API endpoints.

### Troubleshooting MCP серверов

**Общие проблемы**:

1. **Сервер не запускается**
   - Проверьте command и args
   - Убедитесь что зависимости установлены
   - Проверьте логи

2. **Timeout ошибки**
   - Увеличьте Network Timeout
   - Проверьте сетевое подключение
   - Проверьте URL сервера

3. **Аутентификация не работает**
   - Проверьте API ключи в env
   - Проверьте headers
   - Проверьте права доступа

4. **Tools не обнаруживаются**
   - Перезапустите сервер
   - Проверьте конфигурацию
   - Проверьте логи сервера

### Platform-Specific конфигурация

#### Windows

На Windows используйте Windows Command Prompt (cmd):

```json
{
  "mcpServers": {
    "puppeteer": {
      "type": "stdio",
      "command": "cmd",
      "args": [
        "/c",
        "npx",
        "-y",
        "@modelcontextprotocol/server-puppeteer"
      ]
    }
  }
}
```

**Особенности Windows**:
- Используйте `cmd` как command
- Добавьте `/c` в args
- Затем команда для выполнения


## Примеры использования

### Базовый workflow

```bash
# Запуск Kilo Code
kilocode

# Начать задачу
> Implement user authentication with JWT

# Kilo Code в Code mode создаст реализацию
# Одобрите операции или используйте Ctrl+Y для YOLO mode

# Переключиться в Debug mode для тестирования
Shift+Tab

> Test the authentication flow

# Переключиться в Ask mode для вопросов
Shift+Tab

> Explain how the JWT token validation works
```

### Orchestrator workflow

```bash
# Запуск в Orchestrator mode
kilocode
/mode orchestrator

# Дать сложную задачу
> Build a complete e-commerce checkout system with:
> - Shopping cart
> - Payment processing
> - Order confirmation
> - Email notifications

# Orchestrator разобьет на подзадачи:
# 1. Architect mode: Design system architecture
# 2. Code mode: Implement shopping cart
# 3. Code mode: Integrate payment gateway
# 4. Code mode: Build order system
# 5. Code mode: Setup email service
# 6. Debug mode: Test entire flow

# Каждая подзадача выполняется в своем контексте
# Orchestrator получает только резюме
```

### Code Review workflow

```bash
# Запуск с конкретной задачей
kilocode

> Review the changes in the last commit for:
> - Security vulnerabilities
> - Performance issues
> - Code quality
> - Best practices

# Или использовать Ask mode для non-invasive review
/mode ask

> Analyze this pull request and provide feedback
```

### Debugging workflow

```bash
# Переключиться в Debug mode
kilocode
/mode debug

> The authentication is failing with "Invalid token" error
> Help me find and fix the issue

# Debug mode систематически:
# 1. Анализирует код
# 2. Проверяет логи
# 3. Тестирует гипотезы
# 4. Находит root cause
# 5. Предлагает fix
```

### Architecture Planning workflow

```bash
# Использовать Architect mode
kilocode
/mode architect

> Design a microservices architecture for a social media platform
> with the following requirements:
> - User service
> - Post service
> - Comment service
> - Notification service
> - Real-time messaging

# Architect mode создаст:
# 1. High-level architecture diagram
# 2. Service boundaries
# 3. API contracts
# 4. Data flow
# 5. Technology recommendations
# 6. Implementation plan
```

### Continuous workflow

```bash
# День 1: Начать feature
kilocode
> Implement user profile page

# День 2: Продолжить
kilocode -c
> Add profile picture upload

# День 3: Продолжить
kilocode -c
> Add profile editing functionality

# День 4: Review и finish
kilocode -c
/mode ask
> Review all changes and create a summary
```

### Parallel development

```bash
# Terminal 1: Feature A
kilocode
> Implement payment processing

# Terminal 2: Feature B
kilocode
> Implement email notifications

# Terminal 3: Bug fix
kilocode
> Fix the login redirect issue

# Все работают параллельно
# Каждый на своей git ветке
# Review и merge независимо
```

## Лучшие практики

### 1. Выбор правильного режима

- **Code mode**: Для реализации и изменений
- **Ask mode**: Для вопросов и обучения
- **Architect mode**: Для планирования и дизайна
- **Debug mode**: Для отладки и диагностики
- **Orchestrator mode**: Для сложных multi-step задач

### 2. Использование Skills

Создавайте skills для:
- Стандартов команды
- Повторяющихся workflow
- Специфичных для проекта знаний
- Best practices

### 3. Auto-Approval конфигурация

Начните консервативно:
- Разрешите безопасные команды (`npm install`, `git status`)
- Запретите опасные команды (`rm -rf`, `sudo`)
- Постепенно добавляйте в whitelist

### 4. Checkpoint Management

- Регулярно проверяйте checkpoints
- Используйте для экспериментов
- Быстрый rollback при проблемах

### 5. Task History

- Используйте поиск для нахождения похожих задач
- Фильтруйте по стоимости для оптимизации
- Сохраняйте избранные задачи

### 6. Session Continuation

- Используйте `-c` для продолжения работы
- Сохраняет контекст и историю
- Эффективно для multi-day задач

### 7. Parallel Mode

- Используйте для независимых задач
- Драматически ускоряет разработку
- Каждый инстанс на своей ветке

### 8. MCP Integration

- Добавляйте только нужные серверы
- Настройте auto-approval для trusted tools
- Мониторьте network timeouts

### 9. Model Selection

Выбирайте модель по задаче:
- **Planning**: Быстрые модели (Gemini Flash, Claude Haiku)
- **Implementation**: Мощные модели (Claude Sonnet, GPT-4)
- **Review**: Средние модели (Claude Sonnet)
- **Simple tasks**: Бюджетные модели (Llama, Mistral)

### 10. Cost Optimization

- Используйте `/tasks sort most-expensive` для анализа
- Выбирайте подходящие модели
- Настройте auto-approval для снижения overhead
- Используйте Orchestrator для разбиения задач

## Troubleshooting

### CLI не запускается

**Проблема**: `kilocode: command not found`

**Решение**:
```bash
# Переустановить
npm install -g @kilocode/cli

# Проверить установку
which kilocode
kilocode --version
```

### Ошибка аутентификации

**Проблема**: "Invalid API key"

**Решение**:
```bash
# Проверить конфигурацию
kilocode config

# Или установить через env
export OPENROUTER_API_KEY=sk-or-v1-...
kilocode
```

### Режим не переключается

**Проблема**: Режим не меняется при `Shift+Tab`

**Решение**:
- Используйте `/mode <name>` команду
- Проверьте что не в режиме ввода
- Перезапустите CLI

### Checkpoint не восстанавливается

**Проблема**: "Checkpoint not found"

**Решение**:
```bash
# Проверить список
/checkpoint list

# Использовать полный hash
/checkpoint restore <full-hash>
```

### MCP сервер не работает

**Проблема**: MCP tools недоступны

**Решение**:
1. Проверить конфигурацию: `kilocode config`
2. Перезапустить сервер через UI
3. Проверить логи
4. Увеличить network timeout

### Session не продолжается

**Проблема**: `kilocode -c` не находит сессию

**Решение**:
- Убедитесь что в правильной директории
- Проверьте `.kilocode/` директорию
- Начните новую сессию если нужно

---

*Документация актуальна на январь 2026*
*Источник: https://kilocode.ai*
