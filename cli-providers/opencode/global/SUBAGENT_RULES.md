# ОБЯЗАТЕЛЬНЫЕ ПРАВИЛА ДЛЯ ВСЕХ СУБАГЕНТОВ

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО #1: ЗАПРЕТ РЕКУРСИИ

**СТРОГО ЗАПРЕЩЕНО вызывать task с собственным именем агента!**

### ❌ НЕПРАВИЛЬНО (вызываешь сам себя):
```
// Я backend-developer
task → @backend-developer: "Исправь ошибку"  // ЗАПРЕЩЕНО!
```

### ✅ ПРАВИЛЬНО (исправляешь сам или возвращаешь оркестратору):
```
// Я backend-developer, нашёл ошибку

Вариант 1 - Исправляю сам:
1. У меня есть write/edit
2. Исправляю ошибку
3. Проверяю (npm run dev)
4. Возвращаю результат оркестратору

Вариант 2 - Не могу исправить:
1. Возвращаю статус "failed" с описанием проблемы
2. Оркестратор решит кого вызвать (debugger, другого специалиста)
```

### Почему это важно?
- ❌ Рекурсия = зацикливание
- ❌ Искажение контекста
- ❌ Бесконечный цикл делегирования
- ✅ Оркестратор контролирует поток

---

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО #2: RETRY С ФИКСАЦИЕЙ

**Работай до достижения цели, но с ограничениями:**

### Алгоритм работы:

```
1. Получил задачу от оркестратора
   ↓
2. Попытка #1: Выполняю задачу
   ↓
3. Проверка: npm run dev / lint / build
   ↓
4. Ошибка? → Попытка #2 (исправляю)
   ↓
5. Ошибка? → Попытка #3 (исправляю)
   ↓
6. Ошибка? → СТОП! Возврат оркестратору
   ↓
7. Успех? → Фиксирую результат → Возврат оркестратору
```

### Максимум 3 попытки!

**Если после 3 попыток не получилось:**
```json
{
  "status": "failed",
  "attempts": 3,
  "last_error": "TypeError: Cannot read property 'id' of undefined",
  "files_changed": ["src/api/users.ts"],
  "recommendation": "Нужен @debugger для глубокого анализа"
}
```

### Фиксация результата:

**Перед возвратом оркестратору:**
1. ✅ Все файлы сохранены
2. ✅ Проверка пройдена (lint/build/dev)
3. ✅ Логи просмотрены
4. ✅ Нет критических ошибок

**Не возвращай оркестратору "полуготовый" код!**

---

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО #3: ФОРМАТ ОТВЕТА

**ВСЕГДА возвращай результат в JSON формате:**

```json
{
  "status": "success" | "failed" | "partial" | "blocked" | "needs_input",
  "agent": "твоё имя",
  "task": "краткое описание задачи",
  "files_changed": ["список изменённых файлов"],
  "files_created": ["список созданных файлов"],
  "tests_passed": true | false,
  "lint_passed": true | false,
  "build_passed": true | false,
  "errors": ["список ошибок если есть"],
  "warnings": ["список предупреждений"],
  "attempts": 1-3,
  "next_action": "ready_for_qa" | "needs_fix" | "done" | "needs_clarification" | "suggests_alternative" | "blocked",
  "notes": "дополнительная информация для оркестратора",
  
  "for_documentarist": {
    "summary": "Кратко что сделано (1-2 предложения)",
    "components_added": ["UserService", "AuthMiddleware"],
    "components_modified": ["ApiRouter"],
    "components_removed": [],
    "dependencies_added": ["zod@3.22"],
    "architecture_changes": "Добавлен слой валидации между API и сервисами",
    "breaking_changes": false,
    "migration_needed": false
  }
}
```

### Поле `for_documentarist` (ОБЯЗАТЕЛЬНО для кодеров)

Это поле читает @documentarist для точного обновления памяти:

| Поле | Описание |
|------|----------|
| `summary` | Что сделано человеческим языком |
| `components_added` | Новые модули/классы/функции |
| `components_modified` | Изменённые компоненты |
| `components_removed` | Удалённые компоненты |
| `dependencies_added` | Новые зависимости (npm/pip/etc) |
| `architecture_changes` | Изменения в архитектуре (если есть) |
| `breaking_changes` | Есть ли breaking changes |
| `migration_needed` | Нужна ли миграция БД |

**Пример:**
```json
"for_documentarist": {
  "summary": "Добавлен REST API для управления пользователями с валидацией через Zod",
  "components_added": ["UserController", "UserService", "userSchema"],
  "components_modified": ["AppRouter"],
  "dependencies_added": ["zod@3.22"],
  "architecture_changes": null,
  "breaking_changes": false,
  "migration_needed": false
}
```

---

### Расширенные статусы:

| Статус | Когда использовать |
|--------|-------------------|
| `success` | Задача выполнена полностью |
| `failed` | Не удалось выполнить после 3 попыток |
| `partial` | Часть задачи выполнена, часть нет |
| `blocked` | Заблокирован внешней зависимостью |
| `needs_input` | Нужно уточнение от пользователя |

### Расширенные next_action:

| Action | Когда использовать |
|--------|-------------------|
| `ready_for_qa` | Код готов к тестам и ревью |
| `needs_fix` | Нужно исправление (передать debugger) |
| `done` | Полностью завершено |
| `needs_clarification` | Недостаточно контекста |
| `suggests_alternative` | Предлагаю лучший способ |
| `blocked` | Жду разблокировки |

---

## 🧠 УМНЫЕ СЦЕНАРИИ

### Сценарий 1: Предлагаю альтернативу

Если видишь лучший способ решения:

```json
{
  "status": "needs_input",
  "next_action": "suggests_alternative",
  "notes": "Вместо создания нового API, лучше расширить существующий /api/users. Это уменьшит дублирование.",
  "alternatives": [
    {"option": "A", "description": "Создать новый endpoint /api/profiles", "pros": ["Изоляция"], "cons": ["Дублирование"]},
    {"option": "B", "description": "Расширить /api/users", "pros": ["DRY"], "cons": ["Сложнее"]}
  ]
}
```

### Сценарий 2: Недостаточно контекста

Если не понимаешь что делать:

```json
{
  "status": "needs_input",
  "next_action": "needs_clarification",
  "notes": "Не понимаю какой формат данных ожидается",
  "questions": [
    "Какой формат даты использовать: ISO или timestamp?",
    "Нужна ли пагинация для списка?",
    "Какой лимит записей по умолчанию?"
  ]
}
```

### Сценарий 3: Заблокирован

Если внешняя зависимость недоступна:

```json
{
  "status": "blocked",
  "next_action": "blocked",
  "blocker": {
    "type": "external_dependency",
    "name": "npm registry",
    "error": "ETIMEDOUT",
    "suggestion": "Попробовать позже или использовать кэш"
  }
}
```

### Сценарий 4: Обнаружил критическую проблему

Если нашёл проблему НЕ связанную с текущей задачей:

```json
{
  "status": "partial",
  "next_action": "ready_for_qa",
  "notes": "Задача выполнена, но обнаружена критическая проблема",
  "discovered_issues": [
    {
      "severity": "critical",
      "file": "src/auth/login.ts",
      "issue": "SQL injection vulnerability",
      "recommendation": "Срочно исправить через @security-auditor"
    }
  ]
}
```

### Сценарий 5: Задача слишком большая

Если задача требует разбиения:

```json
{
  "status": "needs_input",
  "next_action": "needs_clarification",
  "notes": "Задача слишком большая для одного агента",
  "suggested_subtasks": [
    {"task": "Создать модели данных", "agent": "@database-architect"},
    {"task": "Создать API endpoints", "agent": "@backend-developer"},
    {"task": "Создать UI компоненты", "agent": "@frontend-developer"}
  ]
}
```

### Сценарий 6: Несколько вариантов решения

Если не уверен какой вариант лучше:

```json
{
  "status": "needs_input",
  "next_action": "suggests_alternative",
  "notes": "Есть несколько способов реализации, нужен выбор",
  "options": [
    {"option": "REST API", "effort": "low", "scalability": "medium"},
    {"option": "GraphQL", "effort": "medium", "scalability": "high"},
    {"option": "tRPC", "effort": "low", "scalability": "high"}
  ]
}
```

---

**Успех:**
```json
{
  "status": "success",
  "agent": "backend-developer",
  "task": "Создать API для пользователей",
  "files_changed": ["src/api/users/create.ts", "src/api/users/index.ts"],
  "files_created": ["src/types/user.ts"],
  "tests_passed": true,
  "lint_passed": true,
  "build_passed": true,
  "errors": [],
  "warnings": ["File src/api/users/create.ts is 280 lines (close to limit)"],
  "attempts": 1,
  "next_action": "ready_for_qa",
  "notes": "API готов, добавлены логи, всё работает"
}
```

**Неудача:**
```json
{
  "status": "failed",
  "agent": "backend-developer",
  "task": "Исправить баг в auth.ts",
  "files_changed": ["src/auth/login.ts"],
  "tests_passed": false,
  "lint_passed": true,
  "build_passed": true,
  "errors": ["TypeError: Cannot read property 'id' of undefined at line 45"],
  "warnings": [],
  "attempts": 3,
  "next_action": "needs_fix",
  "notes": "Не смог найти причину ошибки, нужен @debugger"
}
```

---

## 📋 ЧЕКЛИСТ ПЕРЕД ВОЗВРАТОМ ОРКЕСТРАТОРУ

- [ ] Задача выполнена (или 3 попытки исчерпаны)
- [ ] Все файлы сохранены
- [ ] Проверка пройдена (lint/build/dev)
- [ ] JSON результат подготовлен
- [ ] ❌ НЕ вызывал task с собственным именем
- [ ] ✅ Готов вернуть результат оркестратору

---

## 🎯 ПОМНИ

**"Не могу выполнить" ≠ "Вызову сам себя"**

**"Не могу выполнить" = "Верну failed оркестратору"**

**Оркестратор решает кого вызвать дальше, не ты!**


---

## 🚨 КРИТИЧЕСКОЕ ПРАВИЛО #4: ЛЕНИВАЯ ЗАГРУЗКА СКИЛЛОВ

**НЕ загружай все скиллы сразу! Загружай ТОЛЬКО когда нужен конкретный паттерн.**

### ❌ НЕПРАВИЛЬНО (загружаешь всё сразу):
```bash
# При старте агента
skill(name="debugging-strategies")
skill(name="error-resolver")
skill(name="error-handling-patterns")
skill(name="memory-forensics")
skill(name="verification-loop")
# ... ещё 10 скиллов
```

### ✅ ПРАВИЛЬНО (загружаешь по необходимости):
```
1. Получил задачу
2. Проанализировал контекст
3. Определил что нужен конкретный паттерн
4. skill(name="error-resolver")  # ОДИН скилл
5. Применил паттерн
6. Если не помогло → skill(name="debugging-strategies")  # следующий
```

### Алгоритм:

```
Задача → Анализ → Нужен паттерн? 
                      ↓ ДА
              Загрузи ОДИН скилл
                      ↓
              Применил → Помогло?
                      ↓ НЕТ
              Загрузи СЛЕДУЮЩИЙ скилл
```

### Почему это важно?
- ❌ Все скиллы сразу = раздутый контекст
- ❌ Раздутый контекст = потеря фокуса
- ❌ Потеря фокуса = плохие решения
- ✅ Один скилл = точечное решение

### Таблица в промпте агента - это СПРАВОЧНИК, не инструкция к загрузке!

---

## 🧠 ПРАВИЛО #5: САМООБУЧЕНИЕ (continuous-learning)

**Все агенты с `continuous-learning` скилом могут запросить извлечение паттерна.**

### Когда запрашивать извлечение:

```
✅ Извлекай когда:
- Решил нетривиальную проблему (>10 мин исследования)
- Нашёл workaround для неочевидного бага
- Обнаружил паттерн, которого нет в документации
- Нашёл оптимальное решение после нескольких попыток

❌ НЕ извлекай:
- Простые lookup в документации
- Одноразовые решения
- Очевидные вещи
```

### Как запросить:

В поле `for_documentarist` добавь:

```json
{
  "for_documentarist": {
    "summary": "...",
    "learning_candidate": {
      "type": "pattern|workaround|optimization|error-fix",
      "description": "Краткое описание что узнал",
      "context": "В каком контексте применимо",
      "solution": "Что именно сработало"
    }
  }
}
```

### Что происходит дальше:

1. Оркестратор видит `learning_candidate`
2. Вызывает `@learning-extractor` с контекстом
3. Learning-extractor создаёт новый skill или обновляет существующий
4. Skill доступен всем агентам в следующих сессиях

**Это делает систему умнее с каждой задачей.**
